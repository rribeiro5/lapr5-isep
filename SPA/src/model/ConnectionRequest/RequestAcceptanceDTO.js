export default class RequestAcceptanceDTO {
    constructor(answer,connectionStrength,tags) {
        this.answer = answer
        this.connectionStrength = connectionStrength
        this.tags = tags
    }
}